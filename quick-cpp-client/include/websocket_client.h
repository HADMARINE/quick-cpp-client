#pragma once
#include <cpprest/ws_client.h>

namespace quick_client {
	namespace web_socket {
		using namespace std;
		using namespace web;
		using namespace web_socket;
		using namespace concurrency;
		
		// 수신 받을 정보에 대한 내용을 추가하기 위한 설정값
		struct {
			string 
		};
	}
}