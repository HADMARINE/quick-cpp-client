#include <string>
#include <optional>
#include <nlohmann/json.hpp>
// #

namespace quick_client {
    namespace tcp {
        using namespace std;
        using namespace web;
        // using namespace 

        using json = nlohmann::json;

        websocket_client
        
        class ws_instance {
            string url;
        };
    }
}